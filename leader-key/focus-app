#!/bin/bash
# Usage: focus-app /Applications/Zen.app
# Opens (or focuses) the app then warps the mouse to its window.
set -euo pipefail

if [[ $# -eq 0 ]]; then
    echo "Usage: focus-app <app-path>" >&2
    exit 1
fi

open "$1"
exec ~/.local/bin/mouse-to-window
